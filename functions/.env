# Non-secret Cloud Functions params (see firebase-functions/params).
# Never put secrets in this file — those go through `firebase functions:secrets:set`.

# "From" address for order-status emails (confirmed / ready for pickup).
# On the asianle.ca domain, which is verified in Resend (SPF/DKIM).
RESEND_FROM_EMAIL=orders@asianle.ca
